# electron-ebook-sync

## installazione
`yarn global add electron`  OR `npm i -g electron`
### client
* `cd client`
* `yarn` OR `npm install`
### server
* `cd server`
* `yarn` OR `npm install`

## utilizzo
### client
* `electron ./client/`

### server
* `node ./server/main.js`